#helm install --namespace infra elasticsearch elastic/elasticsearch -f ./elasticsearch/values.yaml
#helm install --namespace infra kibana elastic/kibana
---
# Permit co-located instances for solitary minikube virtual machines.
antiAffinity: "soft"

# Shrink default JVM heap.
esJavaOpts: "-Xmx512m -Xms512m"
replicas: 2
minimumMasterNodes: 2
# Allocate smaller chunks of memory per pod.
resources:
  requests:
    cpu: "1000m"
    memory: "1024M"
  limits:
    cpu: "1000m"
    memory: "1024M"

# Request smaller persistent volumes.
volumeClaimTemplate:
  accessModes: [ "ReadWriteOnce" ]
  storageClassName: "standard"
  resources:
    requests:
      storage: 1000M