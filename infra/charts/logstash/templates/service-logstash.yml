apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  namespace: {{.Values.global.baseNamespace}}
  labels:
    app: {{ .Values.name }}
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: {{ .Values.resources.port }}
  selector:
    app: {{ .Values.name }}
